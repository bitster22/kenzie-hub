import { styled } from "styled-components";

export const StyledInfo = styled.div`
  padding: 2rem 0;
  display: flex;
  justify-content: space-between;
  color: var(--grey-0);
  p {
    color: var(--grey-1);
  }
  flex-wrap: wrap;
`;
